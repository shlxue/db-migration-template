include = "{$include,.gitignore,\.*.standalone_migrations,\.standalone_migrations}"

excludeFiles = "{*.{sqlite3,zip}}"
exclude = "{$exclude,*.{lock,sqlite3}}"
# exclude               = "{Rakefile,README,config.ru,Gemfile.lock}"
# excludeDirectories    = "{tmp,log,doc,script}"
# excludeInFileChooser  = "{tmp,log,doc,script,.DS_Store,.gitkeep}"
# excludeInFolderSearch = "{tmp,log,doc,script,.DS_Store,Gemfile.lock}"

[source]
softTabs = true
tabSize = 2
softWrap = false
tabSize = 2
wrapColumn = 120

[ "{*.standalone_migrations}" ]
fileType         = source.yaml